{
  "name": "allkeyshop-best-deals-fix",
  "version": "0.5.1",
  "private": true,
  "description": "Userscript that restores AllKeyShop's Best Deals catalogue ranking.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "check": "npm run build && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
