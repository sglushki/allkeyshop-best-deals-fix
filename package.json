{
  "name": "allkeyshop-best-deals-fix",
  "version": "0.4.0",
  "private": true,
  "description": "A userscript that reconstructs AllKeyShop's broken Best Deals catalogue sort from its existing deal-score data.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "check": "npm run build && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
