{
  "name": "allkeyshop-best-deals-fix",
  "version": "0.3.1",
  "private": true,
  "description": "A focused userscript compatibility shim for AllKeyShop's broken Best deals catalogue sort.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "check": "npm run build && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
